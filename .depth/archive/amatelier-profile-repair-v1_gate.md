TASK: amatelier-profile-repair-v1
SCOPE: non-trivial
FILES: src/amatelier/engine/evolver.py, src/amatelier/engine/therapist.py, src/amatelier/paths.py, src/amatelier/agents/elena/traits.json, src/amatelier/agents/marcus/traits.json, src/amatelier/agents/clare/traits.json, src/amatelier/agents/simon/traits.json, src/amatelier/agents/naomi/traits.json
REPLACES: amatelier/engine/therapist.py `_parse_outcomes` TRAIT: field handling (line 781) — still present but needs defensive filter so stale "none (...)" status markers never reach Emerging Traits. Trait authority logic is missing entirely; porting from claude-suite/engine/evolver.py:419-682 and claude-suite/engine/therapist.py:232-358.
MIGRATION: none — no new callers. Existing therapist flow keeps working; trait section is only appended when the new `_should_evaluate_traits(case_notes)` predicate is True (every ≥10 observations since last review).
CALLERS: therapist.run_session() calls _build_trait_section(agent_name, obs_bundle) when _should_evaluate_traits(case_notes) returns True, appending the trait-review prompt to the agent brief. _apply_outcomes() calls evolver.apply_trait_action(agent, verdict, rt_id) for each TRAIT: verdict parsed from SESSION OUTCOMES. No other callers for v1.
USER_PATH: RT completes → digest written → run_therapist(digest_path) iterates agents → per agent: load case_notes → check _should_evaluate_traits → if True, _load_obs_bundle(agent, 10) → _build_trait_section() appends TRAIT REVIEW block to brief → therapist conversation produces SESSION OUTCOMES with TRAIT: verdicts → _parse_outcomes filters "none (...)" → _apply_outcomes iterates verdicts, calls evolver.apply_trait_action → evolver reads agents/{agent}/traits.json, applies evidence gate (≥3 RTs + ≥2 signal types + RT existence in observations/), writes back, resets obs_since_last_trait_review counter, re-renders Emerging Traits section in CLAUDE.md. Failure path: missing obs dir → empty bundle → skip trait section; malformed verdict → logged, skipped, other verdicts continue.
RED_STATE: (1) evolver.py has no trait gating machinery — 8 functions missing: _traits_path, _load_traits, _save_traits, _observation_rt_ids, apply_trait_action, _reset_trait_counter, _render_traits_section, render_traits_section. (2) therapist.py has no trait-review threshold — 5 functions missing: TRAIT_REVIEW_THRESHOLD const, _should_evaluate_traits, _load_obs_bundle, _load_traits_file, _build_trait_section. (3) therapist.py `_parse_outcomes` line 781 accepts any non-"none" TRAIT: value including "none (Phase 3 pending)" — same bug as Claude Suite pre-fix. (4) No traits.json files exist at agents/{agent}/traits.json — evolver has nowhere to write. (5) Agent CLAUDE.md Emerging Traits sections are empty (Phase 1 strip complete) with no mechanism to populate them.
RED_TYPE: INFRASTRUCTURE
GREEN_CONDITION: (a) `python -c "from amatelier.engine import evolver; print(hasattr(evolver, 'apply_trait_action'), hasattr(evolver, 'render_traits_section'))"` prints "True True". (b) `python -c "from amatelier.engine import therapist; print(therapist.TRAIT_REVIEW_THRESHOLD, hasattr(therapist, '_should_evaluate_traits'), hasattr(therapist, '_build_trait_section'))"` prints "10 True True". (c) `python -c "from amatelier.engine.therapist import _parse_outcomes; msg=[{'role':'therapist','message':'SESSION OUTCOMES\nTRAIT: none (Phase 3 pending)\n'}]; print(_parse_outcomes(msg))"` returns trait as empty string. (d) All 5 agents have agents/{agent}/traits.json files with empty confirmed[], candidate[], rejected[], history[]. (e) `python -c "from amatelier.engine.evolver import render_traits_section; render_traits_section('elena')"` runs without error and the Emerging Traits section in elena/CLAUDE.md remains empty (no traits to render).
OMISSIONS: (1) No observer script in this gate — that's Phase 3 (separate gate). (2) No traits pre-seeded — agents ship clean (user directive: "they must go out clean, no phase 4"). (3) No tests added beyond smoke commands in GREEN_CONDITION. (4) No Phase 4 behavior consolidation or trait application (user directive). (5) traits.json schema is copied from claude-suite verbatim — no amatelier-specific fields. (6) _load_obs_bundle points at agents/{agent}/observations/ which does not yet exist per agent; function returns empty list gracefully until the Phase 3 observer script populates it.
