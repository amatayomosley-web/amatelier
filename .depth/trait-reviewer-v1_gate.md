TASK: trait-reviewer-v1
SCOPE: non-trivial
FILES: src/amatelier/engine/sonnet_observer.py, src/amatelier/engine/therapist.py
REPLACES: `_write_obs` at sonnet_observer.py:874 currently writes obs file and returns path; no side effect on case_notes. `_increment_obs_counters` exists at therapist.py:230 but has zero callers. Combined result: `obs_since_last_trait_review` never increments → `_should_evaluate_traits` always returns False → trait-review branch in `run_session` is dead code in practice.
MIGRATION: none — no new callers. Existing observer + therapist pipeline keeps working; counter bump is a side-effect added to `_write_obs`. `_increment_obs_counters` stays (used by standalone scripts / batch use cases) but receives no new wiring in this gate.
CALLERS: sonnet_observer._write_obs (called by _observe_one_and_write at line 148 and line 181) calls a new helper `_bump_trait_counter(agent)` after successful file write. `_bump_trait_counter` reads case_notes via therapist._load_case_notes, bumps `obs_since_last_trait_review` by 1, writes back via therapist._save_case_notes. Guarded by try/except so a case_notes write failure never breaks observation persistence. No other new callers.
USER_PATH: RT completes → runner calls `observe_rt(rt_id, transcript, workers, ...)` → for each agent the model returns an obs dict → `_write_obs(agent, rt_id, obs)` writes obs-{rt_id}.json AND bumps agent's case_notes counter by 1 → after N such writes the counter reaches TRAIT_REVIEW_THRESHOLD (10) → next therapist run for that agent calls `_should_evaluate_traits(case_notes)` which returns True → `_load_obs_bundle(agent, 10)` reads last 10 obs files → `_build_trait_section` appends TRAIT REVIEW block → therapist session produces TRAIT: verdict → `_apply_outcomes` calls `evolver.apply_trait_action` → evolver applies evidence gate, writes traits.json, calls `_reset_trait_counter(agent, rt_id)` which zeroes `obs_since_last_trait_review`. Failure paths: (a) case_notes write raises → logged, obs file still persists, counter misses one tick (self-healing next RT); (b) obs file write fails → counter NOT bumped (invariant: counter reflects successful writes only).
RED_STATE: (1) sonnet_observer.py:_write_obs (line 874) has no counter-bump side effect. (2) therapist.py:_increment_obs_counters (line 230) has zero callers verified by `grep -rn _increment_obs_counters src/` returning only the def line. (3) As a consequence, 2 existing obs files for elena (obs-test-rt-id.json, obs-verify-rt.json) wrote successfully but elena's case_notes `obs_since_last_trait_review` is still 0 — verified by reading C:/Users/willi/AppData/Local/amatelier/agents/elena/case_notes not yet bumping.
RED_TYPE: INFRASTRUCTURE
GREEN_CONDITION: (a) `python -c "from amatelier.engine import sonnet_observer; print(hasattr(sonnet_observer, '_bump_trait_counter'))"` prints "True". (b) Smoke test: starting from a clean case_notes with `obs_since_last_trait_review=0`, calling `sonnet_observer._write_obs('naomi', 'smoke-rt', {'agent':'naomi','rt_id':'smoke-rt','cognitive_moves':[],'behavior_signals':[]})` and then reading case_notes shows counter == 1. (c) Repeat the call 9 more times with distinct rt_ids → counter == 10 → `therapist._should_evaluate_traits(case_notes)` returns True. (d) Calling `_write_obs` with an invalid agent (directory unwritable) raises as before and does NOT bump any counter. (e) `grep -n "_bump_trait_counter" src/amatelier/engine/sonnet_observer.py` shows the new helper defined and called from `_write_obs`.
OMISSIONS: (1) No changes to `_increment_obs_counters` — it stays as-is for possible future batch use. (2) No changes to observer's batch/single LLM call paths — only `_write_obs`. (3) No new tests beyond GREEN_CONDITION smoke commands. (4) No cleanup of the 2 pre-existing elena test obs files — their counter "debt" stays missed (user-acceptable: test data). (5) No change to `_reset_trait_counter` or evolver evidence-gate logic. (6) Live RT run to confirm end-to-end trait-review firing is deferred to a follow-up — this gate only proves the counter wires up.

VERIFICATION STATUS: COMPLETE
- (a) _bump_trait_counter helper exists — PASS
- (b) single _write_obs call bumps counter 0→1 — PASS
- (c) 10 writes → counter=10, _should_evaluate_traits returns True — PASS
- (d) failed _write_obs (OSError) did not bump counter (10→10) — PASS
- (e) grep shows def at line 874, call at line 902 — PASS
- Smoke test state cleaned (test obs files removed, naomi counter reset to 0)
